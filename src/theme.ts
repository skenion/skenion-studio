import { createTheme } from "@mantine/core";

export const theme = createTheme({
  fontFamily: "var(--font-family-sans), system-ui, sans-serif",
  fontFamilyMonospace: "var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace",
  defaultRadius: 0,
  radius: {
    xs: "0px",
    sm: "0px",
    md: "0px",
    lg: "0px",
    xl: "0px"
  }
});
